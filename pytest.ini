[pytest]
markers =
    live: makes real network/auth calls against the live POMS server (needs POMS_CLIENT_DIR + a valid token, via `source setup.sh`). Read-only. Excluded by default; run with `pytest -m live`.
addopts = -m "not live"
